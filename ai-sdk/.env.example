# Local LM Studio (default) — liefert logprobs
LLM_PROVIDER=lmstudio
LLM_MODEL=lfm2-1.2b-rag
LLM_PROVIDER_URL=http://localhost:1234/v1

# Ein Lauf misst immer beide Modi (mit/ohne Reasoning) und schreibt beide nach
# dist/<modell>.md. LLM_REASONING wählt nur, welcher Modus zusätzlich die
# ausführliche Token-Ausgabe im Log bekommt.
# Bei Providern ohne Reasoning-Schalter (ollama/gateway) legt es den Modus fest.
LLM_REASONING=false

# Local vLLM — liefert logprobs
# LLM_PROVIDER=vllm
# LLM_MODEL=Qwen/Qwen3-8B
# LLM_PROVIDER_URL=http://localhost:8000/v1
# RUNPOD_API_KEY=your-api-key

# Local Ollama — keine logprobs
# LLM_PROVIDER=ollama
# LLM_MODEL=qwen3:8b
# LLM_PROVIDER_URL=http://localhost:11434

# Vercel AI Gateway (https://vercel.com/ai-gateway)
# LLM_PROVIDER=gateway
# LLM_MODEL=alibaba/qwen-3-14b
# AI_GATEWAY_API_KEY=your-api-key
