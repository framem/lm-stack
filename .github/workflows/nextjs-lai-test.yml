name: Next.js LAI Tests

on:
  push:
    paths:
      - 'nextjs-lai/**'
      - '.github/workflows/nextjs-lai-test.yml'
  pull_request:
    paths:
      - 'nextjs-lai/**'
      - '.github/workflows/nextjs-lai-test.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: nextjs-lai
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: nextjs-lai/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run test
